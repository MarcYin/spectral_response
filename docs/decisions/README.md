# Decisions

Store short design records here.

Recommended pattern:

- one file per decision
- short statement of context
- explicit decision
- implementation consequence

The first decision that should be captured is the canonical split between `sampled_curve` and `band_spec`.
