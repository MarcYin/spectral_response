# Sensor Notes

Store per-sensor ingestion and QA notes here.

Each note should capture:

- source choice
- parser assumptions
- validation results
- unresolved caveats

The remaining P2 optical backlog and current blocker note are summarized in `p2_optical_bucket.md` and `prisma.md`.
