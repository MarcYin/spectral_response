# Validation Scripts

Place standalone validation and reporting scripts here.

Examples:

- batch sensor validation
- QA report generation
- plot export utilities
- regression fixture refresh

Overlay reference convention for sampled-curve sensors:

- `sources/extracted/<sensor_unit_id>/<representation_variant>/overlay_reference.csv`
- columns: `band_id,wavelength_nm,response`
